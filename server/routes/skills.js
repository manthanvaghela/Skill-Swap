const express = require('express');
const router = express.Router();

// Placeholder for skills routes
router.get('/', (req, res) => {
  res.json({ message: 'Skills route - coming soon' });
});

module.exports = router; 