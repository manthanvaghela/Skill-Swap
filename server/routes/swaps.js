const express = require('express');
const router = express.Router();

// Placeholder for swaps routes
router.get('/', (req, res) => {
  res.json({ message: 'Swaps route - coming soon' });
});

module.exports = router; 